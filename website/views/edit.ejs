<!DOCTYPE html>
<html>
<head><title>Edit Article</title></head>
<body>
  <h2><%= article._id ? 'Edit' : 'New' %> Article</h2>
  <form method="POST">
    <input name="title" placeholder="Title" value="<%= article.title || '' %>" required><br>
    <textarea name="content" placeholder="Content" rows="10" cols="50" required><%= article.content || '' %></textarea><br>
    <button>Save</button>
  </form>
  <a href="/dashboard">Back</a>
</body>
</html>